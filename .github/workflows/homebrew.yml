name: Homebrew

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  formula:
    name: Formula
    runs-on: macos-latest
    env:
      FORMULA: shellcheck-readability
      TAP: yowainwright/shellcheck_readability
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false

      - name: Tap formula
        run: brew tap "$TAP" "$GITHUB_WORKSPACE"

      - name: Trust tap
        run: brew trust "$TAP"

      - name: Install formula from HEAD
        run: brew install --HEAD --build-from-source "$FORMULA"

      - name: Test formula
        run: brew test "$FORMULA"

      - name: Audit formula
        run: brew audit --formula "$FORMULA"
